name: Publish to WinGet

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        required: true
        type: string

permissions:
  contents: read

jobs:
  publish-winget:
    runs-on: ubuntu-latest
    steps:
      - name: Determine release tag
        id: release
        run: |
          tag="${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.event.release.tag_name }}"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"
      - uses: cargo-bins/cargo-binstall@main
      - run: cargo binstall komac -y
      - name: Sync WinGet fork
        env:
          GH_TOKEN: ${{ secrets.WINGET_TOKEN }}
        run: gh repo sync bgreenwell/winget-pkgs
      - name: Submit manifest pull request
        env:
          GH_TOKEN: ${{ secrets.WINGET_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.WINGET_TOKEN }}
        run: |
          url="https://github.com/bgreenwell/git-ego/releases/download/${{ steps.release.outputs.tag }}/git-ego-${{ steps.release.outputs.tag }}-windows-x86_64.zip"

          if gh api repos/microsoft/winget-pkgs/contents/manifests/b/bgreenwell/GitEgo > /dev/null 2>&1; then
            komac update bgreenwell.GitEgo \
              --version "${{ steps.release.outputs.version }}" \
              --urls "$url" \
              --submit
            exit 0
          fi

          komac new bgreenwell.GitEgo \
            --version "${{ steps.release.outputs.version }}" \
            --urls "$url" \
            --publisher bgreenwell \
            --publisher-url https://github.com/bgreenwell \
            --package-name git-ego \
            --package-url https://github.com/bgreenwell/git-ego \
            --license MIT \
            --license-url "https://github.com/bgreenwell/git-ego/blob/${{ steps.release.outputs.tag }}/LICENSE" \
            --short-description "Git identity manager" \
            --release-notes-url "https://github.com/bgreenwell/git-ego/releases/tag/${{ steps.release.outputs.tag }}" \
            --submit
